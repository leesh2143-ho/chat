{
  "name": "realtime-chat-server",
  "version": "1.0.0",
  "description": "A real-time chat server supporting multiple rooms.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
